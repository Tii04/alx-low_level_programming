#include "main.h"

/**
 * _islower - checks if character int c is lowercase
 * @c: A character argument.
 * Return: 1 if int c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

