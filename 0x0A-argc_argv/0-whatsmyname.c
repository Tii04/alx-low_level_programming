#include <stdio.h>

/**
 * main - prints program name.
 * @argc: count of cl arguments.
 * @argv: string array.
 * Return: string array.
 */

int main(int argc, char *argv[])
{
	printf("%s\n",argv[0]);
	(void)argc;
	return (0);
}
