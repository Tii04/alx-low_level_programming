#include <stdio.h>
#include <stdlib.h>

/**
 * main - adds two numbers.
 * @argc: counter.
 * @argv: vector.
 * Return: int or error.
 */

int main(int argc, char *argv[])
{
	int i, sum = 0;

	if (argc == NULL)
	{
		return(0);
	}
	
}

