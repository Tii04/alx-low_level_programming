#include <main.h>

/**
 * main - prints a _putchar, followed by a new line.
 * Return: 0 if exited properly, non-zero number otherwise.
 */

int main(void)
{
	char a;

	a = "_putchar";
	return (a);
}
